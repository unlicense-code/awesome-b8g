// the isolate component/module returns a context so it is like 
// the vm module in nodejs.
// Build Instructions for Isolate and a basic main
const build = {
  "x64/debian/graaljs": "Dockerfile" 
  "x64/debian/v8": "Dockerfile" 
}
